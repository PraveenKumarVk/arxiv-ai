[phases.setup]
nixPkgs = ["python312", "poppler_utils", "tesseract"]

[phases.install]
cmds = [
    "pip install uv",
    "uv sync --frozen --no-dev"
]

[phases.build]
cmds = []

[start]
cmd = "uvicorn src.main:app --host 0.0.0.0 --port $PORT --workers 2"
